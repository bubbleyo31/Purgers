# 武器、瞄準與射擊架構

> 最後核對：2026-09-14  
> 相關文件：`40_職業與特殊能力.md`、`60_傷害生命死亡與回饋.md`、`70_第一人稱模型與動畫.md`

## 控制流程

```text
NetInput
→ PlayerWeaponController
   ├─ AttackRifle
   └─ SupportSMG

NetInput → PlayerAimController → Aim Phase / FOV / Animator
NetInput → PlayerQuickActionController → 當前職業 Quick Action

武器命中
→ WeaponHitZone / DamageReceiverUtility
→ DamageRequest / DamageResult
→ Feedback Relay
```

## 程式職責

| 程式 | 職責 | 主要連結 |
|---|---|---|
| `PlayerWeaponController.cs` | 依職業路由主要武器，處理射擊、換彈、武器中斷與 Action Gate | AttackRifle、SupportSMG、Profession、Aim、Quick Action |
| `PlayerAimController.cs` | Aim 請求與階段、FOV Request、封鎖判斷；換彈期間不能瞄準 | Action Gate、FOV Manager、ViewModel Aim Animator |
| `PlayerQuickActionController.cs` | Quick Action 階段與能力調度，必要時中斷武器 | Runtime Manager、Action Gate、Weapon Controller |
| `PlayerActionBlockTypes.cs` | 定義封鎖 Mask、來源與武器中斷原因 | Action Gate、兩把槍、能力 |
| `PlayerActionGate.cs` | 集中計算目前被哪些來源封鎖哪些行為 | 武器、Aim、Quick Action、職業能力 |
| `AttackRifle.cs` | Attack 步槍射擊、彈藥、換彈、Hitscan、散布、射速、世界槍聲、Tracer、DamageRequest、Hit Override／Shot Modifier | Weapon Controller、Damage、Audio、Feedback、ViewModel |
| `SupportSMG.cs` | Support SMG 射擊、彈藥、換彈、特殊空中模式、世界槍聲、Tracer、DamageRequest | Support Aerial、Weapon Controller、Damage、Audio |
| `RifleHitOverride.cs` | 定義 `RifleHitContext` 與 `IRifleHitOverride`，讓職業能力改寫步槍命中用途 | AttackRifle、Support Healing |
| `BulletTracerLineDrawer.cs` | 本地／表現層繪製射擊 Tracer | AttackRifle、SupportSMG |
| `WeaponHitZone.cs` | 掛在碰撞部位，描述一般／暴頭等武器命中區 | Damage Hit Zone、兩把槍 |
| `WeaponDamage.cs` | 武器 Hit Zone 類型的相容定義／映射 | `WeaponHitZone`、Damage System |
| `FocusTarget.cs` | Attack Focus 可選目標與生命狀態入口 | AttackFocusAbility、EnemyActor、CombatDeathHandler |

## 已確定規則

- Attack Rifle 與 Support SMG 換彈期間都不能進 Aim。
- Aim 動畫正播到瞄準姿勢；取消 Aim 以負速度倒播。
- Aim 後停用的是 ViewModel 的 Idle Motion、Walk Bob 等程序位移，不是把整個 `FirstPersonViewModelMotionProfile` 永久改為零或停用所有表現。
- 武器 Networked 屬性只能在 `Spawned()` 後讀取。ViewModel 切職業時若新武器尚未 Spawn，必須安全等待。
- 槍聲需要讓其他玩家聽見時走 `NetworkPlayerAudioEmitter`，而且 One Shot 必須跟隨射手，避免玩家移動後聲音留在舊世界座標。
- Animation Event 的拉槍機等細節音只給本機第一人稱，不進世界廣播。

## 擴充武器時

1. 先新增可由 `PlayerWeaponController` 路由的武器介面或來源，不要把新職業硬塞進 Attack Rifle 分支。
2. 彈藥 HUD 實作 `IPlayerWeaponHUDSource`。
3. 傷害仍產生 `DamageRequest`；不要另建扣血流程。
4. 世界聲音走 Network Emitter，本地機械細節走 Animation Audio Events。
5. ViewModel 動畫只讀武器公開狀態，並處理尚未 Spawn 的安全條件。

## 2026-09-17 局部核對與架構補充

AttackRifle／SupportSMG 的最近有效命中與距離衰減統一委派 `WeaponHitUtility`。保留自身 Collider 排除、最近世界遮擋、相同距離保留先遇到者及既有插值規則。各職業傷害、治療、換彈、特殊模式和 Networked 狀態仍由原模組負責。

## 變更紀錄

- 2026-09-14：建立武器與瞄準架構基線。


- 2026-09-17：同步本輪局部修正、資產設定與驗證邊界。
