# 場景、工具、選單與其他輔助程式

> 最後核對：2026-09-18  
> 相關文件：`10_網路連線與輸入.md`

## 場景與選單

| 程式 | 職責 |
|---|---|
| `GameLogic.cs` | 遊戲場景玩家生成、Spawn Point、死亡重生、職業保存與離線清理 |
| `MenuConnection.cs` | Quick Play／Fresh Host／Continue／Client Join 的存檔責任與 Fusion 連線入口 |
| `MenuConnectionBehaviour.cs` | 連線流程的 Unity／Fusion Behaviour 包裝 |
| `MenuSaveFlowController.cs` | Continue Overlay 列出、選擇、載入存檔並以 Host 開房 |
| `MenuSaveLaunchPolicy.cs` | 純計算分類四種 Menu 啟動意圖 |
| `MenuSceneLaunchPolicy.cs` | 連線前校正已過期的 Fusion Menu 場景選擇 |
| `MenuSaveSlotRow.cs` | 單一存檔列的文字、時間與選中狀態 |
| `MenuUIController.cs` | 選單畫面、按鈕與連線狀態 UI |
| `SafeHouseFlowController.cs` | Host 互動、全員 Ready 與 State Authority 切入 Game |
| `SafeHouseStartTerminalView.cs` | 開始裝置範圍與 World Space E 提示 |
| `SafeHouseHudController.cs` | StageLevel 與 Ready Check HUD |

Phase 1-B 不修改 Photon FusionMenu 套件 View。既有 Quick Play 仍送出空 Session／`Creating=false`，由專案 `MenuConnection` 改判為新存檔 Host；Continue UI 則使用 `FusionMenuUIMain` 公開的 ConnectionArgs／Connection／Controller。`_Menu` 已在 Scene Instance 建立 Continue 按鈕與 Sorting Order 200 的最上層 Overlay，動態列使用 `Assets/Prefabs/UI/Menu/SaveSlotRow.prefab`。按鈕事件在 `MenuSaveFlowController.Awake()` 接管，避免複製 Quick Play 後殘留的 Persistent Call 同時觸發新遊戲。

Phase 2 新增 `Assets/Scenes/SafeHouse.unity` 灰盒場景。Build Settings 順序為 `_Menu`、`SafeHouse`、`Game`，MenuConfig 的唯一可選啟動場景為 SafeHouse。場景含四個 PlayerSpawnPoint、開始裝置、World Space TMP、Ready HUD、Directional Light 與預覽 Camera。`MenuSceneLaunchPolicy` 會處理舊 PlayerPrefs 仍指向 Game 的情況，不修改 Photon 套件原碼。

## 關卡與編輯工具

| 程式 | 職責 |
|---|---|
| `CourseGenerator.cs` | 關卡／路線生成工具的 Runtime 資料與執行入口 |
| `CourseGeneratorEditor.cs` | `CourseGenerator` 的自訂 Inspector／Editor 操作；只存在 Editor 組件 |
| `ShowNavmesh.cs` | 顯示 NavMesh 的開發輔助工具 |
| `AutoAddMeshCollider.cs` | 自動替目標 Mesh 補上 MeshCollider 的場景建置工具 |

## 測試與展示元件

| 程式 | 職責 |
|---|---|
| `NetworkPingPongMover.cs` | 網路物件往返移動測試／場景機關範例 |
| `EnemyPatrolAreaTestSpawner.cs` | F4 Enemy 測試生成；詳細見 `91_敵人攻擊表現與生成.md` |

## 相機通用元件

| 程式 | 職責 |
|---|---|
| `CameraFollow.cs` | 相機跟隨目標；被 Local View、觀戰與 Grapple Visual 使用 |
| `FirstPersonFovManager.cs` | 第一人稱多來源 FOV 修改仲裁 |
| `FirstPersonHitCameraShake.cs` | 命中結果相機震動 |

## 維護原則

- Editor 程式維持在 `Assets/Scripts/Editor` 或 Editor-only Assembly，避免進 Build。
- Debug Gizmo 不可在 Runtime 每幀產生 GC 或更改 AI 狀態。
- 測試生成與測試移動物件不能被正式波次、任務或存檔系統依賴。
- `GameLogic` 若繼續擴張到回合、Encounter、計分與結算，應拆成各自 Controller，不要變成全域 God Object。

## 2026-09-17 局部核對與架構補充

MapGeneration/Prototype 的 MapChunk／MapConnector 提供區塊與接點資料。MapRunSelectionPrototype 在 Marker 模式使用固定 Seed 並支援 F9 重抽；Fusion 模式每個 Host Runner 產生一次新的 Run Seed，從四個入口隨機固定本輪出生點，再從其餘三個方向抽選出口。同一輪死亡重生沿用入口。ConnectorAlignmentPrototype 依 Run Seed 從 Host Chunk Prefab 候選池與四個入口方向抽選，生成並對齊唯一一個第二 Chunk；目前候選池只有 MapChunk_Prototype，因此現階段隨機差異主要是連接方向與旋轉。對齊成功後，MapConnectorBlockerPrototype 保留第一 Chunk 出口與第二 Chunk 入口形成的接合點，其餘六個 Connector 均生成阻擋屋，包含玩家出生入口。每個 Connector 可指定 Blocker Spawn Point 來校正建築 Prefab 的 Pivot 與朝向；留空時使用 Connector Transform。Blocker Local Position Offset 以錨點局部 X／Y／Z 調整實際生成位置，Scene 與 Prefab 編輯視窗會顯示偏移連線、生成點、朝向與可調尺寸的預覽框。MapChunk Prefab 保存 Editor 預先烘焙的 NavMeshSurface/NavMeshData；阻擋屋生成後，MapRuntimeNavigationPrototype 依兩個 Chunk 的位置與旋轉載入資料，在接合 Connector 建立雙向 NavMesh Link，未使用入口則由阻擋屋的 Carving NavMeshObstacle 封閉。接著系統依 Connector 邊界取樣並為每個 Chunk 建立最大可連通 Ground Patrol Area。地面敵人仍由 Fusion Tick Navigator 使用 NavMesh 查詢，不新增 NavMeshAgent。離線 Marker、F9、N 鍵入口遇任何 NetworkRunner 都不執行。Host 生成的第二 Chunk、阻擋屋與 Runtime Patrol Area 目前仍是普通 GameObject，不會同步到 Client，也不允許第三塊或循環生成。

## 變更紀錄

- 2026-09-14：建立場景與工具架構基線。


- 2026-09-17：同步本輪局部修正、資產設定與驗證邊界。
- 2026-09-18：加入存檔 Phase 1-B Menu Controller 與手動 UI 接線邊界。
- 2026-09-18：加入 SafeHouse 灰盒 Scene、Ready Check、場景校正與跨場景玩家生命週期。
